<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.spectrum.spectrum">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".src.config.GlobalApplication"
        android:allowBackup="true"
        android:icon="@drawable/icon_logo_small"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".src.activities.splash.SplashActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".src.activities.login.LogInActivity"
            android:windowSoftInputMode="adjustResize"
            android:screenOrientation="portrait"/>

        <activity
            android:name=".src.activities.signup.SignUpActivity"
            android:screenOrientation="portrait"/>

        <activity
            android:name=".src.activities.main.MainActivity"
            android:screenOrientation="portrait"/>

    </application>

</manifest>